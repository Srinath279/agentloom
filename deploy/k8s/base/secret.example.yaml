# Template only — never commit a real secret. Create it per environment:
#
#   kubectl -n agentloom-dev create secret generic agentloom-secrets \
#     --from-literal=OPENROUTER_API_KEY=sk-or-v1-...
#
# Optional keys (Langfuse tracing; omit to run without tracing backend):
#   LANGFUSE_HOST, LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY
#
# For GitOps, use SealedSecrets or ExternalSecrets instead of raw manifests.
apiVersion: v1
kind: Secret
metadata:
  name: agentloom-secrets
type: Opaque
stringData:
  OPENROUTER_API_KEY: "sk-or-v1-REPLACE-ME"
