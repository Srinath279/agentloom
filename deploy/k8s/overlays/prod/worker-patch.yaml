apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentloom-worker
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: worker
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              memory: 1Gi
